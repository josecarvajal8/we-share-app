import React from 'react'
import Particles from 'react-particles-js'
import {ParticlesConfig} from '../../Config/Constants'
export default () => {
    return(
        <Particles
          params={ParticlesConfig} />
    )
}